{
  "name": "hellotype",
  "version": "1.0.0",
  "description": "",
  "private": true,
  "scripts": {
    "test": "jest",
    "watch": "jest --watch",
    "coverage": "jest --coverage",
    "clean": "rm -rf ./build/* && rm -rf ./dist/*",
    "dist": "npm run clean && npx webpack",
    "build": "npm run clean && tsc",
    "start": "npm run build && node ./build/app.js",
    "only": "npm run build && node"
  },
  "author": "unkwnow",
  "license": "ISC",
  "dependencies": {},
  "devDependencies": {
    "@types/jest": "^25.1.4",
    "dotenv": "^8.2.0",
    "jest": "^25.1.0",
    "ts-jest": "^25.2.1",
    "ts-loader": "^6.2.1",
    "typescript": "^3.8.3",
    "webpack": "^4.42.0",
    "webpack-cli": "^3.3.11"
  }
}
